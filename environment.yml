name: unirobosim-dev
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - setuptools
  - wheel
  - pip:
      - build==1.5.0
      - mypy==2.3.1
      - pytest==9.1.1
      - pytest-cov==7.1.0
      - ruff==0.16.3
